<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Index page</title>
  </head>
  <body>
    <%- include('partials/navbar') %>
    <div style="display: flex; flex-wrap: wrap; gap: 1rem">
      <% if (allMessages) { %> <% locals.allMessages.map( user =>{%>
      <div
        style="
          border: 1px black solid;
          margin-top: 1rem;
          display: flex;
          position: relative;
          flex-direction: column;
          width: 150px;
          height: 175px;
          padding: 0.5rem;
          overflow: auto;
        "
      >
        <div style="color: rgba(0, 0, 0, 0.35); position: absolute; right: 5px">
          <%= user.id %>
        </div>
        <div style="color: red"><%= user.author %></div>
        <div style="color: yellowgreen; background-color: azure">
          <%= user.message_time %>
        </div>
        <a href="/message?id=<%= user.id %>">View as page</a>
        <div style="margin-top: 6px">Text: <%= user.message %></div>
      </div>

      <% }) %> <% } %>
    </div>
  </body>
</html>
